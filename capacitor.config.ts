import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.example.app',
	appName: 'follow-manager',
	webDir: 'dist',
	bundledWebRuntime: false
};

export default config;
